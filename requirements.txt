# MediAssist AI - Streamlit Cloud Deployment
# Only direct application imports listed.
# Transitive deps (torch, langchain-core, etc.) are resolved by pip/uv automatically.
# streamlit is pre-installed on Streamlit Cloud — do NOT list it here.

# --- Environment ---
python-dotenv>=1.0.0

# --- LangChain (used directly in code) ---
langchain-classic>=1.0.0,<2.0.0
langchain-community>=0.2.0,<1.0.0
langchain-huggingface>=0.1.0,<2.0.0
langchain-groq>=0.1.0,<2.0.0

# --- Embeddings (required at runtime by HuggingFaceEmbeddings) ---
sentence-transformers>=2.0.0

# --- Vector Store ---
faiss-cpu>=1.7.0

# --- PDF Loading (vectorstore rebuild script) ---
pypdf>=3.0.0
