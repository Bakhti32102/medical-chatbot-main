# MediAssist AI - Streamlit Cloud compatible requirements
# Only direct dependencies with minimum version constraints
# Let pip resolve transitive dependencies to avoid conflicts

# --- Core ---
streamlit>=1.28.0
python-dotenv>=1.0.0

# --- LangChain ecosystem ---
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-huggingface>=0.1.0
langchain-groq>=0.1.0
langchain-text-splitters>=0.2.0

# --- LLM / Retrieval chain (classic API) ---
langchain-classic>=1.0.0

# --- FAISS ---
faiss-cpu>=1.7.0

# --- Embeddings ---
sentence-transformers>=2.0.0

# --- PDF loading (for vectorstore rebuild) ---
pypdf>=3.0.0
